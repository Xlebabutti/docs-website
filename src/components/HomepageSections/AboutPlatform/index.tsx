import clsx from "clsx";
import classes from "./index.module.css";
import Layout from "@theme/Layout";

const AboutPlatform = () => {
  return (
    <section
      className={clsx(`blog-list-page`, classes.pageSection)}
      id="about-platform"
    >
      <div className="a">asdsa</div>
    </section>
  );
};

export default AboutPlatform;
