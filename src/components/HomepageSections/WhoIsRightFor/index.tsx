import clsx from "clsx";
import classes from "./index.module.css";

const WhoIsRightFor = () => {
  return (
    <section
      className={clsx(classes.pageSection)}
      id="who-is-right-for"
    ></section>
  );
};

export default WhoIsRightFor;
